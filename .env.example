# Optional only; the standalone API is deterministic without provider credentials.
PERSONHOOD_API_PORT=8099
PERSONHOOD_FRONTEND_PORT=4173
# A future provider adapter may read this value; never commit a real secret.
# PERSONHOOD_EXPLAIN_ENDPOINT=
